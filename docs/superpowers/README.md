# Design specs and implementation plans

Every spec and plan in `specs/` and `plans/` dated before 2026-09-18 has shipped; the
checkboxes in the plans were never ticked, so they say nothing about status. Treat the
files as the design history behind the feature, and the code as the truth.

- `2026-08-17-leaderboards-supabase.sql` is still live: it is the DDL for the three
  Supabase tables the client writes to, hand-run on the project.

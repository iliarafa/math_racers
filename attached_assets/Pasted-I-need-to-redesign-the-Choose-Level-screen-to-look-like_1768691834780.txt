I need to redesign the "Choose Level" screen to look like a slick F1 "Tyre Strategy" menu, while keeping the existing Tire Icons.

Please refactor `style.css`, `index.html`, and `script.js` to implement this Dark Mode design:

### 1. VISUAL THEME
* **Background:** `#121212` (Dark Grey/Black).
* **Text:** White, Uppercase, Technical Font.

### 2. SECTION 1: SESSION MODE (Top)
Replace the old "Solo/VS" icons with a large **Segmented Toggle Switch** at the top of the screen.
* **Style:** A dark pill-shaped container. Two options: "TIME TRIAL" and "HEAD TO HEAD".
* **Active State:** The selected option has a White background with Black text. Unselected is transparent/grey.

### 3. SECTION 2: SELECT COMPOUND (The Levels)
Refactor the difficulty list into **3 Vertical "Tyre Cards"**.
* **Layout:** Flex column. Gap 15px.
* **Content:** Use the **Existing Tire Icons** you already have.

**Card Styling (`.compound-card`):**
* **Background:** Dark Grey (`#1e1e1e`).
* **Border:** 2px solid transparent (changes on selection).
* **Layout:** Flex row (Icon on left, Text on right). Padding: 20px.
* **Corner Radius:** 12px.

**The 3 Levels (Color Logic):**
1.  **ROOKIE (Soft Compound):**
    * **Color:** Red (`#ff3b30`).
    * **Icon:** Your existing tire icon.
    * **Style:** When selected, the border turns **Red** and the card glows slightly.
    * **Subtext:** "Maximum Grip / Forgiving".

2.  **PROFESSIONAL (Medium Compound):**
    * **Color:** Yellow (`#ffcc00`).
    * **Icon:** Your existing tire icon.
    * **Style:** When selected, the border turns **Yellow**.
    * **Subtext:** "Balanced Performance".

3.  **CHAMPION (Hard Compound):**
    * **Color:** White (`#ffffff`).
    * **Icon:** Your existing tire icon.
    * **Style:** When selected, the border turns **White**.
    * **Subtext:** "Low Grip / Difficult".

### 4. INTERACTION
* The user clicks a card to select the difficulty.
* **Visual Feedback:** The selected card should expand slightly (`transform: scale(1.02)`) and the border/text color should light up in the specific compound color (Red/Yellow/White).
* **Start Button:** A "CONFIRM STRATEGY" button appears at the bottom once a selection is made.

### 5. CLEANUP
* Remove any reference to specific drivers (Kimi/Lando/Max) in this view. We are only selecting the "Tyre Compound" (Difficulty).
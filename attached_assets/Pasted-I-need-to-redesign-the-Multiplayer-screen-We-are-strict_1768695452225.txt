I need to redesign the "Multiplayer" screen. We are strictly using a **Light Theme** (White/Clean) for this page, so I can save the dark aesthetic for a toggle later.

Please refactor `style.css` and the Multiplayer HTML structure in `index.html` to implement this **"Official Timing Screen"** design.

### 1. VISUAL THEME (Light Mode)
* **Page Background:** `#f2f2f7` (A very light, premium gray - like iOS settings).
* **Text:** `#000000` (Pitch Black) and `#666666` (Dark Gray for subtext).
* **Font:** Technical, Uppercase headers.
* **Accent Color:** `#ff2800` (F1 Red).

### 2. LAYOUT: THE "ACCESS PASS" CARD
Wrap the login form in a centered, floating card that mimics a physical Paddock Pass.
* **Container:** Centered vertically/horizontally. Width ~400px.
* **Card Background:** `#ffffff` (Pure White).
* **Border:** None (Clean).
* **Radius:** 24px.
* **Shadow:** `0 20px 40px rgba(0,0,0,0.08)` (Soft, high-end shadow).
* **Padding:** 48px.

### 3. HEADER SECTION
Inside the card:
* **Icon:** A black "Globe" or "Flag" icon (SVG or FontAwesome).
* **Title:** "MULTIPLAYER LOBBY" (Black, Bold, Uppercase, Large).
* **Sub-text:** "Connect to global server" (Gray, Small).
* **Divider:** A subtle horizontal line (`border-bottom: 1px solid #eee`) separating the header from the input.

### 4. INPUT FIELD (Driver Name)
Style the input to look clean and modern:
* **Background:** `#f5f5f5` (Light Gray).
* **Text:** Black.
* **Border:** 2px solid transparent.
* **Focus State:** Background turns White, Border turns `#ff2800` (Red).
* **Height:** 56px.
* **Radius:** 12px.
* **Placeholder:** "Enter Driver ID..." (Gray).

### 5. ACTION BUTTONS
Stack the buttons with a clear hierarchy:

**A. Primary Button (Create Room)**
* **Text:** "HOST SESSION".
* **Style:** Bright Red background (`#ff2800`), White Text, Bold.
* **Radius:** 12px.
* **Height:** 50px.
* **Hover:** Darker Red (`#cc0000`).

**B. Secondary Button (Join Room)**
* **Text:** "JOIN EXISTING".
* **Style:** White background, 2px Solid Black Border, Black Text.
* **Radius:** 12px.
* **Height:** 50px.
* **Hover:** Light Gray background.

### 6. FOOTER (Server Status)
* Place a status indicator at the very bottom of the card.
* **Visual:** A small Green Dot (pulsing) + Text "Online Region: EU-West" (Light Gray).

### 7. CSS CLEANUP
* Ensure high contrast between the text and the white background.
* Remove any lingering dark-mode styles for this specific container.
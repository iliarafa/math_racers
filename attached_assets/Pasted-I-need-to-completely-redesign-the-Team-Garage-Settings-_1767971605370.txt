I need to completely redesign the "Team Garage" (Settings) tab. The current UI is messy and difficult to navigate.

Please refactor `style.css` and the Garage HTML structure in `index.html` (or the JS render function) to implement a modern **"F1 Engineering Dashboard"** using a Bento Grid layout.

### 1. NEW LAYOUT STRUCTURE (CSS Grid)
Replace the current vertical list with a Grid Container (`.garage-dashboard`).
* **Columns:** 2 columns.
* **Rows:** Auto-sized.
* **Gap:** 15px.
* **Padding:** 20px.

### 2. THE CARDS (The Content)
Refactor the content into these specific distinct "Cards".
* **Style for all Cards (`.dashboard-card`):**
    * Background: `#1e1e1e` (Dark Grey).
    * Border: 1px solid `#333`.
    * Border-radius: 16px.
    * Padding: 15px.
    * Color: White.
    * Shadow: `0 4px 6px rgba(0,0,0,0.3)`.

**Card A: Driver Profile (Span 2 columns - Full Width)**
* **Content:** Driver Name ("Max Verstappen"), Difficulty Label ("Champion"), and the 4 Team Color circles (The Paint Shop) arranged horizontally.
* **Visual:** Make this the "Header" of the dashboard.

**Card B: Telemetry (Span 2 columns - Full Width)**
* **Content:** The 3 stats (Total Laps, Career Pts, Races Won).
* **Layout:** Flexbox, evenly spaced.
* **Style:** Remove the boxy borders around individual stats. Make them look like digital readouts (Large number, small label below). Use the Team Color for the numbers.

**Card C: Strategy Room (Span 1 column)**
* **Content:** A large icon (e.g., 📊 or 📘) and the text "Strategy Guide".
* **Action:** Clicking the whole card opens the Strategy View.
* **Style:** Make it look like a clickable app icon.

**Card D: Reflex Training (Span 1 column)**
* **Content:** A large icon (e.g., ⚡️) and the text "Reflex Test".
* **Action:** Clicking the whole card opens the Reaction Game.
* **Style:** Same as Strategy Room.

**Card E: Race Briefing (Span 2 columns)**
* **Content:** Title "Race Regulations".
* **Action:** Initially just shows the title and a "Read" button. Clicking it expands to show the rules text (Points, Penalties, Overtakes).
* **Why:** This saves space compared to the current huge text block.

**Card F: Pit Console (Span 2 columns)**
* **Content:** The "Pit Radio" (Sound) toggle and the "Retire Car" (Reset) button.
* **Layout:** Flexbox, space-between.
* **Style:** Make the "Retire Car" button smaller and less intrusive (e.g., a text link or small outline button) so it isn't accidentally clicked.

### 3. CSS AESTHETICS (The "Slick" Feel)
* **Background:** The main page background should be very dark (`#121212`).
* **Typography:** Use a clean sans-serif font. Make headers uppercase and small (tracking wide).
* **Accents:** Use the CSS variable `--primary-color` (set by the Paint Shop) for active states, toggle switches, and stat numbers.
* **Interaction:** Add a subtle `transform: scale(0.98)` effect when clicking any card.

### 4. IMPLEMENTATION
* Clear the old "Garage" HTML.
* Inject this new Grid structure.
* Ensure all buttons (Strategy, Reflex, Reset) still trigger their correct functions.
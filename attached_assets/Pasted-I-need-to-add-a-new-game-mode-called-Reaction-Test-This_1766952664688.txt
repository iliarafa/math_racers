I need to add a new game mode called "Reaction Test". This replicates the F1 start procedure.

Please update `index.html`, `style.css`, and `script.js` to implement the following:

### 1. HTML STRUCTURE (The Start Lights)
Create a new view container `<div id="reaction-view">`. Inside it, place:
* **The Lights:** A container with 5 circular divs. By default, they are "Off" (Dark Gray).
* **The Button:** A large, clickable area or button labeled "LAUNCH" (or "Hold Clutch").
* **The Result:** A text display for the time (e.g., "0.000s").
* **Back Button:** To return to the main menu.

### 2. CSS STYLING
* **Lights Container:** Use Flexbox to align the 5 lights horizontally. Black background box to look like the gantry.
* **The Lights:**
    * Width/Height: 50px.
    * Border-radius: 50% (Circle).
    * Default Color: `#333` (Dark Gray).
    * Class `.light-on`: Background color `#ff0000` (Bright Red) with a glow effect (`box-shadow`).
* **Launch Button:** Make it large and easy to tap.

### 3. JAVASCRIPT LOGIC
Create a function `initReactionTest()` and handle the game state:

**State A: Idle**
* User clicks "Start Sequence".

**State B: The Sequence**
* Turn on Light 1 (Red). Wait 1 second.
* Turn on Light 2 (Red). Wait 1 second.
* Turn on Light 3 (Red). Wait 1 second.
* Turn on Light 4 (Red). Wait 1 second.
* Turn on Light 5 (Red).
* *Crucial Step:* Once all 5 are on, wait a **Random amount of time** (between 0.2s and 3.0s).

**State C: Lights Out (GO!)**
* Turn OFF all lights simultaneously (remove `.light-on` class).
* Capture `const startTime = Date.now()`.

**State D: User Input**
* User clicks the "GO" button.
* **If clicked BEFORE lights go out:** Display "JUMP START! (Disqualified)" in red.
* **If clicked AFTER lights go out:**
    * Capture `const endTime = Date.now()`.
    * Calculate `const reactionTime = (endTime - startTime) / 1000`.
    * Display result exactly like this: `Reaction Time: ${reactionTime.toFixed(3)}s`.

**4. INTEGRATION**
* Add a button on the Main Menu labeled "Reflex Training" that opens this new view.